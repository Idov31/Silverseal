[package]
name = "Silverseal"
version = "1.0.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
log = "0.4.29"
uefi = { version = "0.36.1", features = ["logger", "panic_handler", "global_allocator", "alloc"] }
